# This are things I learned while doing this project


## Maven useful commands

- `mvn clean` Clean target directory of the project
- `mvn package` Compile the source code, run tests and package it to a JAR file
- Commands can be aggrouped ex: `mvn clean package`


## Docker

- Microservices doesnt need individual Dockerfiles